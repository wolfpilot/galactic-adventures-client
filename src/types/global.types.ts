export enum DistanceUnit {
  kilometres = "km",
  miles = "mi",
}

export enum SpeedUnit {
  kilometresPerHour = "km/h",
  milesPerHour = "mi/h",
}

export enum TemperatureUnit {
  kelvin = "K",
  celsius = "°C",
}
